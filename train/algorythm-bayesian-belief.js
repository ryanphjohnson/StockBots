var belief;

function Init()
{
	//Create Belief
}

function GetAction (stocks)
{
	var actions = [];
	//Loop over Chromosomes and pass in stocks
	//build up list of actions
	return actions;
}

module.exports = {
	Init: Init,
	GetAction: GetAction
}
