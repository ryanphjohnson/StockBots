var net;

function Init()
{
	//Create Neural Net
}

function GetAction (stocks)
{
	var actions = [];
	//Loop over Chromosomes and pass in stocks
	//build up list of actions
	return actions;
}

module.exports = {
	Init: Init,
	GetAction: GetAction
}
